## Fixes

- Corrected stale `Fhir.Metrics` version in `README.md`'s "External Dependencies" table from `1.3.0` to `1.4.0`, and in `docs/type-system-unification-assessment.md`'s comparison table. Added guidance to `CLAUDE.md` and the Copilot dev-guidelines to always update the root README's versioned dependency table (and any `docs/` files that name a version) when bumping a package reference. (#1449, closes #1446)
