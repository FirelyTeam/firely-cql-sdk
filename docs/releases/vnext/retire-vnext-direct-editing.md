## Features

- `docs/releases/vnext-release-notes.md` is now a static pointer doc instead of a directly-editable template; fragment files under `docs/releases/vnext/` are the sole pending-content source going forward. The `cut-release-notes` skill and AI instructions updated accordingly. (#1445)
